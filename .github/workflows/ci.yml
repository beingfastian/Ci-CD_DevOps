name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'  # Use the version of Node.js that suits your project

      - name: Install dependencies
        run: npm install
      
      - name: Run Linter
        run: npm run lint

  sast:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run SAST Tool
        run: echo "Running SAST Tool"  # Replace with actual SAST command or tool

  docker:
    runs-on: ubuntu-latest
    needs: [lint, sast]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}  # Set this in GitHub Secrets
          password: ${{ secrets.DOCKER_PASSWORD }}  # Set this in GitHub Secrets

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: hanzla908/hanzala-web:latest
